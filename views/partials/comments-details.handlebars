<section class="card">
    <header>
      <h3>{{user_id}}</h3>
      <p style="font-size:10px">{{date_created}}</p>
      <br>
      <hr>
      </header>

    <div class="card-body">
      <p>{{comment_text}}</p>
    </div>
  </a>
</section>

